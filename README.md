Sources for the plugin `CoronaProvider.native.popup.activity`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.native.popup.activity" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
